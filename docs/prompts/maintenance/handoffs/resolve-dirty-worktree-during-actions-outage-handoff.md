# Completion Handoff

## Original prompt

There is no point trying to complete the publication while the GitHub server is having issues. Close for now what can be closed and write the handoff.

There are still 76 unstaged changes that need to be committed or cleaned. Focus on that.

## Result

- Stopped the GitHub Actions monitor without bypassing the protected-branch checks.
- Closed superseded Dependabot PR #35 with a pointer to replacement PR #36; PR #36 remains open for protected CI after recovery.
- Removed the three temporary dependency/feature worktrees and pruned stale worktree metadata while retaining the branch refs needed for continuation.
- Audited all 76 unstaged/untracked entries.
- Reverted 69 reproducible UI PQ and Help screenshot outputs whose diffs were broad timestamp, temporary-path, hash, and environment-rendering churn. The tracked baselines remain intact and the outputs can be regenerated by CI.
- Verified and committed the five intentional contract-template layout persistence source/test changes as `64238ca` (`Fix contract workspace layout persistence`).
- Preserved and staged the previously written Dependabot resolution prompt/handoff archive instead of deleting it.
- Archived this maintenance request and continuation handoff.

## Files changed

Committed in `64238ca`:

- `isrc_manager/contract_templates/dialogs.py`
- `tests/app/_app_shell_support.py`
- `tests/app/test_app_shell_layout_persistence.py`
- `tests/contract_templates/test_dialogs.py`
- `tests/contract_templates/test_workspace_layout_helpers.py`

Maintenance archives committed with this handoff:

- `docs/prompts/maintenance/resolve-dependabot-prs.md`
- `docs/prompts/maintenance/handoffs/resolve-dependabot-prs-handoff.md`
- `docs/prompts/maintenance/resolve-dirty-worktree-during-actions-outage.md`
- `docs/prompts/maintenance/handoffs/resolve-dirty-worktree-during-actions-outage-handoff.md`

Generated changes reverted:

- `artifacts/ui_pq/**`
- `docs/help/screenshots/**`

## Verification

- `QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest --no-cov -q tests/contract_templates/test_dialogs.py tests/contract_templates/test_workspace_layout_helpers.py tests/app/test_app_shell_layout_persistence.py` — passed.
- `QT_QPA_PLATFORM=offscreen .venv/bin/python -m pytest --no-cov -q tests/contract_templates tests/app/test_app_shell_layout_persistence.py tests/app/test_app_shell_workspace_docks.py` — passed.
- `.venv/bin/python -m ruff check isrc_manager/contract_templates/dialogs.py tests/app/_app_shell_support.py tests/app/test_app_shell_layout_persistence.py tests/contract_templates/test_dialogs.py tests/contract_templates/test_workspace_layout_helpers.py` — passed.
- `.venv/bin/python -m black --check isrc_manager/contract_templates/dialogs.py tests/app/_app_shell_support.py tests/app/test_app_shell_layout_persistence.py tests/contract_templates/test_dialogs.py tests/contract_templates/test_workspace_layout_helpers.py` — five files unchanged.
- `.venv/bin/python -m mypy` — no issues in 50 source files.
- `.venv/bin/python -m compileall -q ISRC_manager.py isrc_manager tests` — passed.
- `git diff --cached --check` and the source/test commit diff check — passed.

Online verification was intentionally not claimed: at handoff time GitHub reported Actions as `major_outage` and required workflows could not reliably start.

## Prompt archive

- `docs/prompts/maintenance/resolve-dirty-worktree-during-actions-outage.md`
- `docs/prompts/maintenance/handoffs/resolve-dirty-worktree-during-actions-outage-handoff.md`

## Follow-up actions

1. Wait until GitHub Actions is operational, then retrigger PR #36 at its current head and require all 11 `Protect Main` checks to pass.
2. Squash-merge PR #36 (`Fix dependency audit and update setup-python`). PR #36 contains `actions/setup-python@v7` and the `cryptography==50.0.0` security fix; local `pip-audit` reported no known vulnerabilities.
3. Fetch `origin/main` and create a clean integration worktree. Do not push the current local `main`, which has diverged from remote history.
4. Cherry-pick the production-safe metadata/ISRC feature commit `061c6a6`, the contract layout commit `64238ca`, and this maintenance archive commit onto the refreshed `origin/main`.
5. Push the clean fast-forward integration to `main`, then monitor every required main check to success. Fix and repush any code failure before declaring publication complete.
6. Confirm that no open pull requests remain and that any `[skip ci]` Help/QA dashboard automation commits have settled successfully.

## Notes

- PR #33 and PR #34 were already squash-merged successfully before the outage.
- PR #35 was closed as superseded by PR #36.
- PR #36 remained open and blocked at head `b4407cc27f3d64530aeba9e972f6e20a219b5674` because no current-head check suite was admitted.
- Delayed CI run `31128037718` was queued for earlier code-equivalent SHA `d3c1982773c821ab851852103bdc3b2eff9548c4`; it does not satisfy the current-head protection requirement.
- The local `main` contains the earlier metadata/ISRC commit `47801ca`. Use improved isolated commit `061c6a6` for final integration because it also keeps the dropped-audio metadata controller below the repository's 1,000-line module limit.
- `isrc_manager/contract_templates/dialogs.py` was already far above the repository's 1,000-line production-module limit before the retained layout fix. Decompose the dockable workspace host into dedicated submodules in a separate, lower-risk refactor.
