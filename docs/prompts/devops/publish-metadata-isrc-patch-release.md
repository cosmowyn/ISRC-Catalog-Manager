# Publish Metadata and ISRC Patch Release

## Category

devops

## Original prompt, cleaned

Trigger an online build so that the application advances to a new version and the latest patch is distributed.

## Context preserved

- Use the repository's supported online version-bump and release-build automation.
- Publish the already verified shared-metadata, canonical ISRC, contract-layout, and dependency fixes from `main`.
- Advance the current `6.1.2` release by one patch version.
- Monitor version generation, cross-platform packages, GitHub Release publication, checksums, and update metadata through completion.

## Redactions

None.
