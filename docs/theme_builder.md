# Theme Builder Guide

This guide mirrors the in-app help chapter `Theme Settings`.

Use `Help > Help Contents` for the integrated manual. This page summarizes the visual customization surface.

## Theme Builder Scope

The Theme page is a full visual builder for the app’s visible surfaces, including:

- typography
- workspace and panel surfaces
- buttons and help buttons
- inputs and state styling
- tables, lists, headers, and selections
- menus, toolbars, dock chrome, tabs, and status surfaces
- media badge icons for stored audio and image records
- geometry controls such as padding, radius, and scrollbar thickness

## Theme Library

The app ships with starter themes and supports:

- load
- save
- import
- export
- reset

Bundled starter themes remain available as baseline presets for further customization.

## Preview And Advanced QSS

The Theme page includes:

- focused live preview
- optional app-wide live application preview while editing
- hint-visibility and preview-pane controls for the builder itself
- advanced QSS for the last selector-level edge cases
- selector reference and autocomplete support inside the advanced editor

## Related In-App Help Topics

- `Theme Settings`
- `Application Settings`
